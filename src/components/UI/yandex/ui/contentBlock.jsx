import React from "react";

/**
 * Блок с контентом. Может быть любой (погода, карта германии?! или еще что-то)
 */
const ContentBlock = ({ title, children }) => {
  return <>{children}</>;
};

export default ContentBlock;
